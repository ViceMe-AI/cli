interface:
  display_name: "ViceMe Creator App"
  short_description: "Connect projects to ViceMe platform capabilities"
  default_prompt: "Use $viceme to inspect this project, bind it to a TEST Creator App, add only the requested capability, and validate the integration without exposing credentials."
policy:
  allow_implicit_invocation: true
