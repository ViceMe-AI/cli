interface:
  display_name: "ViceMe Skill Publisher"
  short_description: "Publish external Skills as shareable ViceMe Agents"
  default_prompt: "Use $viceme to publish this external skill to ViceMe with the current profile unless I explicitly name another profile in this request, and return its stable share link."
policy:
  allow_implicit_invocation: true
