# T2 CLI 全量复审修复交接

## 追溯信息

- 承接原 PR：[ViceMe-AI/cli#4](https://github.com/ViceMe-AI/cli/pull/4)
- 精确复审 head：`c8e5525`
- 产品基线：飞书需求 revision 3427 / 验收 revision 1169
- 分支基线：最新 `dev`

## 必须修复

- 不得把用户自然语言插入带引号的 shell 命令；必须使用结构化 argv/stdin 或其他非 shell 传输。
- 轮询超时时必须保留并返回已经创建的 edit/run ID。
- resume/retry 必须复用同一个逻辑操作和精确 digest-bound action。
- 保持默认人类可读输出与稳定机器可读输出。

## 验收要求

覆盖 shell 元字符、换行和引号注入，覆盖创建后超时恢复及幂等 resume；同步更新内置 Agent Skill 与 CLI 命令文档。

## 同任务修复 PR

- Engine：[#1165](https://github.com/Leizhenpeng/viceme-engine/pull/1165)
- API：[#532](https://github.com/Leizhenpeng/viceme-api/pull/532)
- Web：[#1306](https://github.com/Leizhenpeng/Viceme-Web/pull/1306)
- CLI：[#26](https://github.com/ViceMe-AI/cli/pull/26)

## 修复记录（feat/skill-agent-t2-remediation）

| 复审问题 | 修复 | 关键文件 |
|---|---|---|
| 用户自然语言被插值进带引号 shell 命令 | `job edit` 新增 `--request-stdin`（用户原话经 subprocess stdin 逐字传输）；`job metadata` 新增 `--edits-stdin`（`{"title","description","author"}` JSON）；内置 Agent Skill 明确禁止插值进引号命令行，与既有 `--expression-stdin`/`--payload-stdin` 同一防线 | `internal/command/job.go`、`skills/viceme/SKILL.md`、`references/commands.md`、`statuses.md` |
| 轮询超时丢失已创建的 edit/run ID | `waitPublicationEdit`/`waitSkillPreviewRun` 超时时输出最后回执 + `meta.wait_timed_out=true`，保留 `edit_id`/`preview_run_id`（零次成功 poll 也有占位 ID），恢复用同一 ID 继续 | `internal/command/job.go` |
| resume/retry 复用同一逻辑操作与 digest 绑定 | `job resume --decision` 切换到 API #532 的独立端点 `resolve-confirmation`（payload/candidate/summary 三 digest + decision 全部必填），不再打共享 `resolve` 端点；typed payload action 维持原端点 | `internal/api/client.go`、`internal/api/types.go`、`internal/command/job.go` |
| 人类可读/机器可读输出 | 本地/引导命令使用格式化裸业务 JSON，`skills read` 输出原文；发布协议命令继续使用 JSON Envelope，超时标记复用 `meta.wait_timed_out` | `internal/output/output.go`、`internal/command/root.go` |

## 验收证据

- `TestJobEditStdinPassesNaturalLanguageVerbatim`：引号/`$()`/反引号/换行/分号载荷逐字进 JSON body（注入面覆盖）。
- `TestJobEditRejectsConflictingRequestTransports`、`TestJobMetadataEditsStdinContract`：传输互斥校验 + metadata JSON stdin 合同。
- `TestJobEditTimeoutPreservesCreatedEditID`、`TestJobRunTimeoutPreservesCreatedRunID`：创建后超时恢复，ID 与 `wait_timed_out` 保留。
- `TestJobResume{Confirm,Cancel}PublishSendsDecisionContract`：决议走 `resolve-confirmation` 且 digest 合同完整。
- `go test ./...`（10 包）与 `make skill-check` 全绿；`command-manifest.json` 与 `quality/release-manifest.json` 已重新生成。

## 三轮复审处置（head 852c234 复审：3 个 P1 + 1 个 P2；已合并最新 dev v0.8.0 基线）

| 复审问题 | 处置 |
|---|---|
| P1 回执的权威 inputs 被 JSON 解码静默丢弃 | `api.SkillPreviewRun` 补齐 `inputs` 字段,与 `inputs_digest` 同源展示;`job run-get` 输出实测覆盖。 |
| P1 same-ID 跨进程恢复没有公开命令 | 新增 `job edit-get <pub> <edit-id> [--timeout]` 与 `job run-get <pub> <run-id> [--timeout]`:单读或续等同一逻辑操作,进程重启后凭 ID 恢复,超时仍如实标 `wait_timed_out`;commands.md/statuses.md 同步。 |
| P1 bundled Skill 调用不存在的 `--json` | SKILL.md 的 `job wait` 移除 `--json`(输出本来就是稳定 JSON envelope);命令清单一致性由 `TestCheckedInCommandManifestMatchesCobraSurface` 覆盖。 |
| P2 commands.md 残留 diff3 冲突标记 | 已清理重复标题与 `\|\|\|\|\|\|\|` 残留;`skillcontent.Bundle.Validate` 新增 merge/diff3 marker 拒绝(`TestValidateRejectsMergeConflictMarkers`),`make skill-check` 以后能拦住同类问题。 |

**验收**：same-ID 单读/续等/超时保留 ID(`TestJobEditGet*`、`TestJobRunGet*`)、权威 inputs 展示、marker 拒绝测试;`go test ./...`(11 包)与 `make skill-check` 全绿;清单文件已重新生成。
