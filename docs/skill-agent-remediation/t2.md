# T2 CLI full-review remediation

## Traceability

- Source task: [ViceMe-AI/cli#4](https://github.com/ViceMe-AI/cli/pull/4)
- Exact reviewed head: `c8e5525`
- Product baseline: Feishu requirement revision 3100 and the frozen acceptance baseline
- Branch base: latest `dev`

## Required changes

- Never interpolate user natural language into a quoted shell command. Use structured argv/stdin or another non-shell transport.
- Preserve and return already-created edit/run IDs when polling times out.
- Make resume/retry use the same logical operation and exact digest-bound action.
- Keep human-readable default output and stable machine-readable output.

## Acceptance

Add metacharacter/newline/quote injection tests, timeout-after-create recovery and idempotent resume tests. Update the bundled Agent Skill and CLI command documentation.

