# T1 CLI full-review remediation

## Traceability

- Source task: [ViceMe-AI/cli#10](https://github.com/ViceMe-AI/cli/pull/10)
- Exact reviewed head: `9fae226`
- Product baseline: Feishu requirement revision 3100 and the frozen acceptance baseline
- Branch base: latest `dev`

## Required changes

- Bind process credentials to an expected audience and trusted API origin.
- Never send an injected credential to an arbitrary `VICEME_API_BASE_URL`.
- Preserve local development through an explicit, narrowly controlled localhost/debug path.
- Do not introduce impersonation, “publish for another user”, internal-dev or test-only public commands.

## Acceptance

Add trusted production origin, rejected hostile origin, explicit localhost development and redirect/origin-change tests. Update CLI Skill/README documentation with the final credential behavior.

