# 核心保护 Skill 协议修订

本文件定义未上线协议的 2026-09-20 修订，先文档、后实现，同分支提交到 dev；未完成验证时 PR 保持 Draft。

核心保护模式使用 `PROTECTED`，命令与端点使用 `skill-guidance`/`guidance`。作者私有 SKILL.md、规则、完整工作流、脚本使用说明保存在服务端；消费者只收到通用入口、明确公开的原始脚本/模板/资源。服务端在模型调用前以代码校验试用、直接购买、买断与月付/不限期订阅；买断也不解锁私有正文。

发布参数 `--protect --public-purpose <公开用途> --public-file <相对路径>` 在现有 publication manifest.spec.protection 中构建公开/私有清单；未选择的文件归为私有，私有仅支持 UTF-8 .md/.txt。每个文件恰好分类一次，SKILL.md 必须私有。恢复发布使用既有 Pending 记录保留的保护清单，不引入新的配置文件。目录、ZIP 和远端源使用同一校验，不改变原脚本字节和执行位。

删除根目录 viceme-cloud.json；不再要求 localWorkflow 或公开 WORKFLOW.md。安装身份继续保存在现有 .viceme/runtime.json 与 .viceme/install-manifest.json。通用入口发起当前任务并读取 execution.md，由本地 Agent 执行完整指导。指导应包含前置条件、规则、步骤、脚本参数/输入输出、产物要求与校验。

凭据继续保存在用户本机私有状态，首次无试用安装只获取购买身份。服务端核验凭据和权益；支付成功后沿用原任务恢复。客户端请求保存 requestKey/sessionId/releaseId，网络或进程失败复用原请求；改任务生成新请求键。失败要显示服务端稳定错误码与可操作恢复方式，重试有界，不能重复扣试用。

不实施设备绑定、换机限制、按购买身份限频/并发或人工凭据恢复。保留普通 API 防护、任务幂等与租约。SOURCE 交付作为既有能力保持原行为，不把其本地门禁注入私有业务正文。

验收：真实本地 API、CLI 与独立 Python runtime 安装/授权/恢复，公开文件保真和无私有正文，试用/无试用/订阅路径、原键恢复、跨身份与版本拒绝。真实模型用自有用例对比原文直执行和指导执行，另测改语言、补充与纠正事实。先自测，再子代理整体评审与修复，最后更新 dev PR；不声明生产已上线。
